import styled from 'styled-components';

const StyledFormGroup = styled.div`
  width: 80%;
  margin: 0 auto 48px;

  & > small {
    font-size: 1.3rem;
    font-weight: 200;
    margin: -10px 0 0;
    display: block;
    text-align: left;
  }
`;

export default StyledFormGroup;